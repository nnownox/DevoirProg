using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Exercice6 : MonoBehaviour
{
    /* [SerializeField]
    private List<bool> listeBool= new List<bool> { false, false, false, false, false ,false };

    // Start is called before the first frame update
    void Start()
    {
        int nombreTrue = 0;
        int nombreFalse = 0;
        foreach(bool valeur in listeBool)
        {
            if (valeur == false)
            {
                nombreFalse++;
            }
            else
            {
                nombreTrue++;
            }

        }

        if (nombreTrue == 0 || nombreTrue == nombreFalse || nombreFalse == 0)
        {
            Debug.Log(nombreTrue);
            Debug.Log("Reussite");
            Debug.Log(nombreFalse);
        }
        else
        {
            Debug.Log("Echec");
        }
    }*/
}
